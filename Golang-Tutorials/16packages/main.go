/*
	Package: are collection of go source files that reside in same directory.

	every go application must contain the main function. The main function should reside in the main package.

	package packagename -> This should be the first line of every go source file

	module -> collection of go packages
*/

package main

import "fmt"

func main() {
	fmt.Println("Simple interset calculation")
}
